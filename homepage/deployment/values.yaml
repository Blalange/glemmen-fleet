# Homepage (https://gethomepage.dev) - dashboard for the Glemmen cluster,
# served at https://glem.men. Kubernetes, Longhorn and Prometheus stats are
# read in-cluster; Grafana stats come from the Grafana API.
#
# Homepage reads credentials from two pre-created Secrets in this namespace:
#
#   homepage-grafana  Grafana admin user/password    (keys: username, password)
#   unpoller-unifi    UniFi controller user/password (keys: username, password)
#
# Create them before Fleet rolls out the bundle by copying the existing values:
#
#   kubectl -n homepage create secret generic homepage-grafana \
#     --from-literal=username=admin \
#     --from-literal=password="$(kubectl -n monitoring get secret grafana-admin \
#       -o jsonpath='{.data.admin-password}' | base64 -d)"
#
#   kubectl -n homepage create secret generic unpoller-unifi \
#     --from-literal=username="$(kubectl -n monitoring get secret unpoller-unifi \
#       -o jsonpath='{.data.username}' | base64 -d)" \
#     --from-literal=password="$(kubectl -n monitoring get secret unpoller-unifi \
#       -o jsonpath='{.data.password}' | base64 -d)"

# The chart (2.1.0) still pins Homepage v1.2.0, whose Grafana widget cannot
# read Alertmanager alerts. v2.3.0 supports 'version: 2' with
# 'alerts: alertmanager' below.
image:
  tag: v2.3.0

# ClusterRole/ClusterRoleBinding and ServiceAccount for the Kubernetes widget
# (nodes, pods and metrics.k8s.io). K3s ships metrics-server, so node CPU and
# memory metrics are available out of the box.
enableRbac: true
serviceAccount:
  create: true

# Traefik terminates TLS with the *.glem.men wildcard certificate, which also
# covers the apex domain.
ingress:
  main:
    enabled: true
    ingressClassName: traefik
    hosts:
      - host: glem.men
        paths:
          - path: /
            pathType: Prefix

# HOMEPAGE_ALLOWED_HOSTS must list every Host header Homepage accepts. The pod
# IP is included so requests that bypass the Ingress (port-forward) still work.
env:
  - name: MY_POD_IP
    valueFrom:
      fieldRef:
        fieldPath: status.podIP
  - name: HOMEPAGE_ALLOWED_HOSTS
    value: "glem.men,$(MY_POD_IP):3000"
  - name: HOMEPAGE_VAR_GRAFANA_USER
    valueFrom:
      secretKeyRef:
        name: homepage-grafana
        key: username
  - name: HOMEPAGE_VAR_GRAFANA_PASSWORD
    valueFrom:
      secretKeyRef:
        name: homepage-grafana
        key: password
  - name: HOMEPAGE_VAR_UNIFI_USER
    valueFrom:
      secretKeyRef:
        name: unpoller-unifi
        key: username
  - name: HOMEPAGE_VAR_UNIFI_PASSWORD
    valueFrom:
      secretKeyRef:
        name: unpoller-unifi
        key: password

# Static files from the homepage-assets ConfigMap (see ../assets) are mounted
# under /app/public so Homepage serves them as /logo.svg, /background.webp and
# /about.html, plus custom.css/custom.js at /app/config. The chart's default
# emptyDir for /app/config/logs is merged in by Helm.
persistence:
  logo:
    enabled: true
    type: configMap
    name: homepage-assets
    mountPath: /app/public/logo.svg
    subPath: logo.svg
    readOnly: true
  background:
    enabled: true
    type: configMap
    name: homepage-assets
    mountPath: /app/public/background.webp
    subPath: background.webp
    readOnly: true
  about:
    enabled: true
    type: configMap
    name: homepage-assets
    mountPath: /app/public/about.html
    subPath: about.html
    readOnly: true
  custom-css:
    enabled: true
    type: configMap
    name: homepage-assets
    mountPath: /app/config/custom.css
    subPath: custom.css
    readOnly: true
  custom-js:
    enabled: true
    type: configMap
    name: homepage-assets
    mountPath: /app/config/custom.js
    subPath: custom.js
    readOnly: true

# Homepage bakes settings.yaml into static HTML at image build time. Hitting
# /api/revalidate as the startup probe regenerates that HTML once the pod is
# up, so the mounted settings (layout, background, logo, ...) apply without
# anyone having to click the refresh icon. The other probes stay TCP.
probes:
  startup:
    enabled: true
    custom: true
    spec:
      httpGet:
        path: /api/revalidate
        port: http
      periodSeconds: 5
      timeoutSeconds: 10
      failureThreshold: 60

config:
  # Use the ServiceAccount above instead of a kubeconfig file.
  kubernetes:
    mode: cluster

  disableIndexing: true

  # The links live under services instead.
  bookmarks: []

  settings:
    title: Glemmen K8S cluster
    description: Dashboard for 2ITA sitt Kubernetes-cluster
    theme: dark
    color: slate
    # 'clean' keeps the header fully transparent (no fill, border or blur);
    # assets/custom.css pushes the cluster stats to the right edge.
    headerStyle: clean
    useEqualHeights: true
    hideVersion: true
    disableIndexing: true
    language: nb-NO
    target: _blank
    # Blossom wallpaper served by Homepage itself (homepage-assets ConfigMap);
    # hotlinking imgur is unreliable because browsers get a consent page.
    # The image stays sharp, 'opacity' only controls how much of it shows
    # through the dark overlay. The frosted glass effect is cardBlur (below)
    # plus the styling in assets/custom.css.
    background:
      image: /background.webp
      opacity: 50
    # Blur the service cards so the background shows through them like frosted
    # glass. The header is 'clean' and stays transparent.
    cardBlur: md
    # Longhorn has no public ingress, so the widget talks to the in-cluster
    # frontend service directly.
    providers:
      longhorn:
        url: http://longhorn-frontend.longhorn-system.svc.cluster.local
    layout:
      - Overvåking:
          style: row
          columns: 3
      - Noder:
          style: row
          columns: 3
      - Nettverk:
          style: row
          columns: 1
      - Lenker:
          style: row
          columns: 3

  widgets:
    # Served from the homepage-assets ConfigMap mounted at /app/public/logo.svg.
    - logo:
        icon: /logo.svg
    - kubernetes:
        cluster:
          show: true
          cpu: true
          memory: true
          showLabel: false
          label: Cluster
        nodes:
          show: false
          cpu: true
          memory: true
          showLabel: true
    - longhorn:
        expanded: true
        total: true
        labels: false
        nodes: false

  services:
    - Overvåking:
        - Grafana:
            icon: grafana.png
            href: https://grafana.glem.men
            description: Dashboards, datasources og varsler
            widget:
              type: grafana
              # Grafana 10.4+ needs version 2, and kube-prometheus-stack
              # provisions an Alertmanager datasource named "alertmanager".
              version: 2
              alerts: alertmanager
              url: http://kube-prometheus-stack-grafana.monitoring.svc.cluster.local
              username: "{{HOMEPAGE_VAR_GRAFANA_USER}}"
              password: "{{HOMEPAGE_VAR_GRAFANA_PASSWORD}}"
        - Prometheus:
            icon: prometheus.png
            href: https://grafana.glem.men/explore
            description: Metrikk og scrape targets
            widget:
              type: prometheus
              url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
        - Cluster:
            icon: kubernetes.png
            href: https://grafana.glem.men/d/rYdddlPWk/node-exporter-full
            description: CPU, RAM og nettverk for hele clusteret
            widget:
              type: prometheusmetric
              url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
              metrics:
                - label: CPU
                  query: 100 * (1 - avg(irate(node_cpu_seconds_total{mode="idle"}[2m])))
                  format:
                    type: percent
                    options:
                      maximumFractionDigits: 1
                - label: RAM
                  query: 100 * (1 - sum(node_memory_MemAvailable_bytes) / sum(node_memory_MemTotal_bytes))
                  format:
                    type: percent
                    options:
                      maximumFractionDigits: 1
                - label: Ned
                  query: >-
                    sum(
                      irate(node_network_receive_bytes_total{device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format:
                    type: byterate
                - label: Opp
                  query: >-
                    sum(
                      irate(node_network_transmit_bytes_total{device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format:
                    type: byterate
        - UniFi:
            icon: unifi.png
            href: https://unifi.glem.men
            description: Nettverkskontroller og tilkoblede klienter
            widget:
              type: unifi
              url: https://unifi.glem.men
              username: "{{HOMEPAGE_VAR_UNIFI_USER}}"
              password: "{{HOMEPAGE_VAR_UNIFI_PASSWORD}}"
    # Per-node CPU/RAM/network from node-exporter via Prometheus. The queries
    # filter on the 'node' label, which kube-prometheus-stack attaches to every
    # node-exporter metric through the ServiceMonitor relabeling. Virtual
    # interfaces are excluded and bond slaves are dropped so bonded nodes are
    # not counted twice.
    #
    # Rates use irate() (last two samples) instead of rate(): when a
    # node-exporter pod restarts it gets a new instance IP, which creates new
    # series, and rate() over a window longer than the series history reports
    # badly wrong values (e.g. 70% CPU instead of 20%) until the window fills.
    - Noder:
        - glemmen130:
            icon: linux.png
            href: https://grafana.glem.men/d/rYdddlPWk/node-exporter-full?var-job=node-exporter&var-nodename=glemmen130
            description: CPU, RAM og nettverk
            widget: &node-widget
              type: prometheusmetric
              url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
              metrics:
                - label: CPU
                  query: 100 * (1 - avg(irate(node_cpu_seconds_total{mode="idle",node="glemmen130"}[2m])))
                  format: &percent
                    type: percent
                    options:
                      maximumFractionDigits: 1
                - label: RAM
                  query: 100 * (1 - node_memory_MemAvailable_bytes{node="glemmen130"} / node_memory_MemTotal_bytes{node="glemmen130"})
                  format: *percent
                - label: Ned
                  query: >-
                    sum(
                      irate(node_network_receive_bytes_total{node="glemmen130",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: &byterate
                    type: byterate
                - label: Opp
                  query: >-
                    sum(
                      irate(node_network_transmit_bytes_total{node="glemmen130",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
        - glemmen150:
            icon: linux.png
            href: https://grafana.glem.men/d/rYdddlPWk/node-exporter-full?var-job=node-exporter&var-nodename=glemmen150
            description: CPU, RAM og nettverk
            widget: &node150-widget
              type: prometheusmetric
              url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
              metrics:
                - label: CPU
                  query: 100 * (1 - avg(irate(node_cpu_seconds_total{mode="idle",node="glemmen150"}[2m])))
                  format: *percent
                - label: RAM
                  query: 100 * (1 - node_memory_MemAvailable_bytes{node="glemmen150"} / node_memory_MemTotal_bytes{node="glemmen150"})
                  format: *percent
                - label: Ned
                  query: >-
                    sum(
                      irate(node_network_receive_bytes_total{node="glemmen150",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
                - label: Opp
                  query: >-
                    sum(
                      irate(node_network_transmit_bytes_total{node="glemmen150",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
        - glemmen160:
            icon: linux.png
            href: https://grafana.glem.men/d/rYdddlPWk/node-exporter-full?var-job=node-exporter&var-nodename=glemmen160
            description: CPU, RAM og nettverk
            widget: &node160-widget
              type: prometheusmetric
              url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
              metrics:
                - label: CPU
                  query: 100 * (1 - avg(irate(node_cpu_seconds_total{mode="idle",node="glemmen160"}[2m])))
                  format: *percent
                - label: RAM
                  query: 100 * (1 - node_memory_MemAvailable_bytes{node="glemmen160"} / node_memory_MemTotal_bytes{node="glemmen160"})
                  format: *percent
                - label: Ned
                  query: >-
                    sum(
                      irate(node_network_receive_bytes_total{node="glemmen160",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
                - label: Opp
                  query: >-
                    sum(
                      irate(node_network_transmit_bytes_total{node="glemmen160",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
        - glemmen170:
            icon: linux.png
            href: https://grafana.glem.men/d/rYdddlPWk/node-exporter-full?var-job=node-exporter&var-nodename=glemmen170
            description: CPU, RAM og nettverk
            widget: &node170-widget
              type: prometheusmetric
              url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
              metrics:
                - label: CPU
                  query: 100 * (1 - avg(irate(node_cpu_seconds_total{mode="idle",node="glemmen170"}[2m])))
                  format: *percent
                - label: RAM
                  query: 100 * (1 - node_memory_MemAvailable_bytes{node="glemmen170"} / node_memory_MemTotal_bytes{node="glemmen170"})
                  format: *percent
                - label: Ned
                  query: >-
                    sum(
                      irate(node_network_receive_bytes_total{node="glemmen170",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
                - label: Opp
                  query: >-
                    sum(
                      irate(node_network_transmit_bytes_total{node="glemmen170",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
        - glemmen180:
            icon: linux.png
            href: https://grafana.glem.men/d/rYdddlPWk/node-exporter-full?var-job=node-exporter&var-nodename=glemmen180
            description: CPU, RAM og nettverk
            widget: &node180-widget
              type: prometheusmetric
              url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
              metrics:
                - label: CPU
                  query: 100 * (1 - avg(irate(node_cpu_seconds_total{mode="idle",node="glemmen180"}[2m])))
                  format: *percent
                - label: RAM
                  query: 100 * (1 - node_memory_MemAvailable_bytes{node="glemmen180"} / node_memory_MemTotal_bytes{node="glemmen180"})
                  format: *percent
                - label: Ned
                  query: >-
                    sum(
                      irate(node_network_receive_bytes_total{node="glemmen180",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
                - label: Opp
                  query: >-
                    sum(
                      irate(node_network_transmit_bytes_total{node="glemmen180",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
        - glemmen80:
            icon: linux.png
            href: https://grafana.glem.men/d/rYdddlPWk/node-exporter-full?var-job=node-exporter&var-nodename=glemmen80
            description: CPU, RAM og nettverk
            widget: &node80-widget
              type: prometheusmetric
              url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
              metrics:
                - label: CPU
                  query: 100 * (1 - avg(irate(node_cpu_seconds_total{mode="idle",node="glemmen80"}[2m])))
                  format: *percent
                - label: RAM
                  query: 100 * (1 - node_memory_MemAvailable_bytes{node="glemmen80"} / node_memory_MemTotal_bytes{node="glemmen80"})
                  format: *percent
                - label: Ned
                  query: >-
                    sum(
                      irate(node_network_receive_bytes_total{node="glemmen80",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
                - label: Opp
                  query: >-
                    sum(
                      irate(node_network_transmit_bytes_total{node="glemmen80",device!~"lo|veth.*|br.*|cali.*|flannel.*|cni.*|docker.*|kube.*|nodelocaldns|lhnet.*|wt.*|tun.*|tap.*|wg.*"}[2m])
                      unless on(device, node) label_replace(node_bonding_slaves, "device", "$1", "slave", "(.+)")
                    )
                  format: *byterate
    # Live WAN throughput graph. Homepage has no PromQL graph widget, so this
    # embeds panel 3 ("WAN Throughput") from the UniFi Poller USG Insights
    # dashboard in Grafana. The panel plots the two WAN byte rates (Rx on the
    # negative axis):
    #   rate(unpoller_device_wan_receive_bytes_total{...}[$__interval])
    #   rate(unpoller_device_wan_transmit_bytes_total{...}[$__interval])
    # Grafana must allow embedding and anonymous viewing (configured in
    # kube-prometheus-stack values).
    - Nettverk:
        - WAN-trafikk:
            icon: unifi.png
            href: https://grafana.glem.men/d/4Yo8IZ-Wk/unifi-poller-usg-insights-prometheus
            description: WAN inn/ut for gatewayen
            widget:
              type: iframe
              name: WAN Throughput
              src: https://grafana.glem.men/d-solo/4Yo8IZ-Wk/unifi-poller3a-usg-insights-prometheus?orgId=1&panelId=Default%20(default)$panel-3&kiosk=true&refresh=1m&var-Controller=$__all&var-Site=$__all&var-Gateway=$__all&timezone=browser&var-Smooth=2m&var-DS_PROMETHEUS=prometheus&from=now-3h&to=now
              classes: h-72 sm:h-72 md:h-80 lg:h-96 xl:h-96 2xl:h-96
    - Lenker:
        - Rancher:
            icon: rancher.png
            href: https://rancher.blalange.org
            description: Administrasjon av clusteret
        - GitHub:
            icon: github.png
            href: https://github.com/Blalange/glemmen-fleet
            description: Konfigurasjonen for clusteret
        - Grafana-alarmer:
            icon: alertmanager.png
            href: https://grafana.glem.men/alerting/list
            description: Aktive alarmer og varsler
