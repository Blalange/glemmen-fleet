# kube-prometheus-stack configuration
# Provides Prometheus, Grafana, Alertmanager, and exporters

# Prometheus configuration
prometheus:
  prometheusSpec:
    replicas: 1
    # Prometheus is a StatefulSet: its updateStrategy only supports RollingUpdate
    # or OnDelete (the CRD rejects Recreate). With one replica the old pod is
    # terminated before the new one is created, so the RWO volume is never
    # attached to two pods at once.
    # Scrape configuration
    scrapeInterval: 15s

    # Storage configuration
    retention: 30d
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: monitoring
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 20Gi

    # Resource limits
    resources:
      requests:
        cpu: 500m
        memory: 500Mi
      limits:
        cpu: 2000m
        memory: 2Gi

    # Service monitors selector - match all
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false

    # Enable monitoring of all namespaces
    serviceMonitorNamespaceSelector: {}
    podMonitorNamespaceSelector: {}

# Grafana configuration
grafana:
  enabled: true
  replicas: 1

  # Grafana mounts a ReadWriteOnce volume (monitoring-grafana), so a rolling
  # update can deadlock: the new pod cannot attach the volume while the old pod
  # still holds it (Multi-Attach error). Recreate terminates the old pod first.
  # rollingUpdate must be explicitly null, otherwise the API rejects the change
  # on the existing Deployment (spec.strategy.rollingUpdate is Forbidden when
  # type is Recreate).
  deploymentStrategy:
    type: Recreate
    rollingUpdate: null

  # Use secret for admin password
  admin:
    existingSecret: grafana-admin
    userKey: admin-user
    passwordKey: admin-password

  # Persistence for dashboards
  persistence:
    enabled: true
    storageClassName: monitoring
    size: 2Gi
    existingClaim: monitoring-grafana

  # Grafana resources
  resources:
    requests:
      cpu: 2
      memory: 1Gi
    limits:
      cpu: 4
      memory: 2Gi

  # Ingress configuration
  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - grafana.glem.men

  # Additional data sources
  additionalDataSources: []

# Alertmanager configuration
alertmanager:
  enabled: true
  replicas: 1
  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: monitoring
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 3Gi

    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 256Mi

# Node exporter - collects hardware and OS metrics
nodeExporter:
  enabled: true

# Kube-state-metrics - collects Kubernetes object state
kubeStateMetrics:
  enabled: true

# Prometheus operator
prometheusOperator:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi

# Default rules - pre-configured alerts
defaultRules:
  create: true
  rules:
    alertmanager: true
    etcd: true
    configReloaders: true
    general: true
    k8s: true
    kubeApiserverAvailability: true
    kubeApiserverSlos: true
    kubelet: true
    kubeProxy: true
    kubePrometheusGeneral: true
    kubePrometheusNodeRecording: true
    kubernetesApps: true
    kubernetesResources: true
    kubernetesStorage: true
    kubernetesSystem: true
    kubeSchedulerAlerting: true
    kubeSchedulerRecording: true
    kubeStateMetrics: true
    network: true
    node: true
    nodeExporterAlerting: true
    nodeExporterRecording: true
    prometheus: true
    prometheusOperator: true

# Additional scrape configs (example for custom services)
# prometheus:
#   prometheusSpec:
#     additionalScrapeConfigs:
#       - job_name: 'custom-service'
#         static_configs:
#           - targets: ['service.namespace.svc.cluster.local:8080']
