defaultNamespace: colibri
# The StorageClass/PVC here are Longhorn volumes and must exist before the
# workload starts. The Hugging Face model mount is created inside the pod by
# the hf-fuse sidecar, so it needs no cluster-level driver.
dependsOn:
  - name: glemmen-fleet-longhorn
